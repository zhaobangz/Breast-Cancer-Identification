# Example environment variables (DO NOT commit secrets to the repo)
# Copy this file to `.env` and fill values locally if needed.

# Path to a local Keras model for the Gradio demo
MODEL_PATH=models/model.h5

# Any other keys should be provided via your environment (CI secrets, etc.)
# For local work, leave blank or use local-only files.
